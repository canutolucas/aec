/**
 * Small constants shared between the web and mobile apps.
 */

export const APP_NAME = "AEC";

/** Every date in this system is anchored to this timezone. See @aec/domain's todayInBrazil. */
export const DEFAULT_TIMEZONE = "America/Sao_Paulo";

export const DEFAULT_CURRENCY = "BRL";
