/**
 * Re-exports the shared UI kit under the app's existing import path, so
 * every page that already imports from "@/lib/ui/components" keeps working
 * unchanged now that the components themselves live in @aec/ui.
 */
export * from "@aec/ui";
